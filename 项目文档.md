# 模型智商检测 · 项目文档

> 纯前端工具：用户填自己的 API 密钥和模型名，浏览器直接调用接口做智力测验，自动判分换算参考智商（IQ）。
> 已部署：https://shenxunmiao123-sketch.github.io/iq-test/ （已接入 sub2api 外接菜单，普通用户可见）

## 一、整体架构

```
用户浏览器（GitHub Pages 静态页）
        │  用户自己的 sk 密钥
        ▼
Cloudflare Worker 反代（worker-withered-mountain-2d73.shenxun1234.workers.dev）
        │  注入 CORS 头 / Origin 白名单 / 频控 / 路径白名单
        ▼
api.aimiroute.com（sub2api 中转站，源码不可改）
```

为什么需要 Worker：中转站未开 CORS，浏览器无法直连；Worker 负责加跨域头并做安全防护。
Worker 地址不在页面显示（写死在 `index.html` 的 `DEFAULT_BASE` 常量），但 F12 可见——
安全策略是「让它无害化」而非保密：调它仍需有效密钥，且有 Origin 白名单 + 频控兜底。

## 二、文件说明

| 文件 | 作用 | 改动频率 |
|------|------|---------|
| `index.html` | 页面本体：界面、106 道人工题、抽题/判分/IQ 换算/排行榜逻辑 | 低 |
| `questions.js` | 894 道程序生成题（**由脚本生成，勿手改**） | 随生成器更新 |
| `generate_bank.js` | 题库生成器：按模板出题，答案由公式/穷举保证正确 | 加新题型时改 |
| `worker.js` | Cloudflare Worker 反代源码（线上同步部署） | 极低 |
| `icon.svg` | sub2api 外接菜单图标 | 不动 |
| `README.md` | 部署教程 | 偶尔 |

## 三、核心机制

### 题库与抽题
- 全库 **1000 题** = 106 人工（`index.html` 的 `CURATED_BANK`）+ 894 生成（`questions.js` 的 `GEN_BANK`），合并时按题干去重。
- 每题三个字段：`cat`（series 数列 / logic 逻辑 / verbal 语言 / spatial 图形）、`d`（1 简单 1 分 / 2 中等 1.5 分 / 3 困难 2 分）、`a`（正确答案下标）。
- **每周自动换题**：以「年份+周数」为随机种子，从全库每类抽 50 题（共 200 题）作为本周题库；同周内每次测试再按难度配额（简单 40% / 中等 40% / 困难 20%）随机抽题、打乱选项。
- **计分**：按难度加权正确率换算 IQ = `55 + 正确率 × 90`（50% ≈ 100 分，满分 ≈ 145）。

### 判分容错
模型回答兼容三种格式：只输出字母 / 「答案是X」/ 长思维链（取结论处的字母）。任一题请求失败（401/超时等）→ **立即中止**，弹窗提示人话原因，不出结果、不写排行榜。

## 四、常见维护操作

| 要做什么 | 怎么做 |
|---------|--------|
| 人工加题 | 编辑 `index.html` 的 `CURATED_BANK`，按现有格式加一行，然后重新部署 |
| 批量生成题 | 编辑 `generate_bank.js`（模板 / `TARGETS` 目标数）→ 运行 `node generate_bank.js` → 提交推送 |
| 换中转站地址 | 只改 `worker.js` 顶部 `UPSTREAM`，到 Cloudflare 重新 Deploy；页面不用动 |
| 换 Worker 通道 | 改 `index.html` 的 `DEFAULT_BASE` 常量，提交推送 |
| 调整防刷阈值 | 改 `worker.js` 的 `RATE_PER_MINUTE` / `RATE_PER_DAY`，重新 Deploy |
| 收紧 Origin | 把 `worker.js` 白名单第一行锁死确切域名；上线稳定后删除 `null` 那行 |

**每次改动后**：`git add . && git commit -m "..." && git push`，GitHub Pages 约 1 分钟自动更新。

## 五、部署位置速查

| 东西 | 在哪 | 地址 |
|------|------|------|
| 页面 | GitHub 仓库 `shenxunmiao123-sketch/iq-test`，Pages 从 main 分支发布 | https://shenxunmiao123-sketch.github.io/iq-test/ |
| 反代 | Cloudflare → Workers & Pages → `worker-withered-mountain-2d73` | https://worker-withered-mountain-2d73.shenxun1234.workers.dev |
| 菜单入口 | sub2api 后台 → 外接菜单：名称「模型智商检测」、角色「普通用户」、URL 填上面的页面地址、图标传 `icon.svg` | — |

## 六、故障排查

| 现象 | 原因与处理 |
|------|-----------|
| HTTP 401 INVALID_API_KEY | 密钥错误/过期，或**密钥分组不含该模型**（如公益组没有 gpt-5.5）。换对应分组的密钥 |
| HTTP 403 / 404 | 密钥无权访问该模型 / 模型名拼写错误 |
| Failed to fetch | CORS 被拦：检查 Worker 是否部署、页面是否用了 Worker 地址 |
| 403 origin not allowed | 页面域名不在 Worker 白名单：检查 `ALLOWED_ORIGIN_RULES` |
| 429 | 触发频控（Worker 30 次/分、1000 次/天），稍后再试 |
| 页面还是旧版 | 强制刷新（Ctrl+F5）；GitHub Pages 部署有约 1 分钟延迟 |
| 分数不可比 | 不同期（周）题库难度有波动，横向对比模型请用同一期成绩 |

## 七、安全边界（务必了解）

- 密钥只存用户浏览器 localStorage、只发往 Worker 与中转站，**不经任何第三方**。
- Worker 三层防护：路径白名单（仅 chat/completions、models）、Origin 白名单（非白名单 403 且不转发）、内置频控。不存储不记录任何数据。
- 前端地址无法绝对保密（F12 可见），现有设计已让地址泄露无利可图；**不要往页面加第三方统计/广告脚本**，那是唯一的 XSS 风险口子。
- GitHub 仓库为 Public（页面代码无秘密），请开启账号双因素认证保护仓库写权限。
